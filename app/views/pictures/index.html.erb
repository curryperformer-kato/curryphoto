<p><%= notice %></p>

<h1>ブログ一覧</h1>

<% @pictures.each do |picture| %>
  <p>タイトル:<%= picture.title %></p>
  <p>本文:<%= picture.content %></p>
  <%= link_to "写真の紹介文を編集する", edit_picture_path(picture.id) %>
  <%= link_to '写真を削除する', picture_path(picture.id), data: { confirm: '本当に削除していいですか？' }, method: :delete %>
<% end %>

<%= link_to "写真を投稿する", new_picture_path %>